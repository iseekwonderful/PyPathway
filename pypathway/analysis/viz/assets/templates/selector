<div class="row">
    <div class="input-field col l10">
        <label class="active" for="select">{{ name }}</label>
        <select id="{{ id }}">
            <option value="Default">{{ default }}</option>
            {{ options }}
        </select>
    </div>
</div>
